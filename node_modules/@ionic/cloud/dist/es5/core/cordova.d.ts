import { Device } from './device';
import { Logger } from './logger';
export declare class Cordova {
    device: Device;
    protected logger: Logger;
    constructor(device: Device, logger: Logger);
    load(): void;
    private isAvailable();
}
